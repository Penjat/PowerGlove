import Foundation
import Combine

// PolySythn is an object that contains a collection of pitches that are combined into a single wave
// Volume is handled by an external update, so that it does not need to keep track of its own timer

class PolySynth {
    init(numberPitch: Int) {

    }
}

class SynthPitch {
    var volume: Double = 0.0
}
